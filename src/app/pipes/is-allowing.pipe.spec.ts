import { IsAllowingPipe } from './is-allowing.pipe';

describe('IsAllowingPipe', () => {
  it('create an instance', () => {
    const pipe = new IsAllowingPipe();
    expect(pipe).toBeTruthy();
  });
});
